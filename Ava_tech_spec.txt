1) AuctionItem class
- String webpageLink, nft item, double startPrice, double endPrice, int blocks, int endBlock, user auctionHolder, user winningUser
- nft should be less than 2 GB
- startPrice should be larger than 1 matic
Methods
- bid(user bidder, double price) checks if blocks less than endBlock
  - if so, renew price

2) User class
- String walletLink, String buyerAddress

3) Dashboard
- ArrayList currentAuctions stores all 30 active AuctionItems
- HashMap pastAuctions stores past 20 <AuctionItem, winning User>
- ArrayList pastWinners stores all past Users that won an auction
Methods
- createAuctionItem () checks if there are 30 AuctionItems in current Auctions
    - if not, creates an AuctionItem Object with all necessary info, adds it to currentAuctions
- closeAuction() completes transaction, assigns bidder as winningUser, moves AuctionItem from currentAuctions to pastAuctions, adds winningUser to pastWinnters
  - if pastWinners exceeds 200, show Vote button

**Not sure how to code the vote to shut down thing. Apologies

